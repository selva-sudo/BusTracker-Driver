<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="App_EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/rounded_edit_text</item>
        <item name="android:textColor">#808080</item>
        <item name="android:layout_marginStart">@dimen/email_side_margin</item>
        <item name="android:layout_marginLeft">@dimen/email_side_margin</item>
        <item name="android:layout_marginEnd">@dimen/email_side_margin</item>
        <item name="android:layout_marginRight">@dimen/email_side_margin</item>
        <item name="android:padding">@dimen/email_side_margin</item>
        <item name="android:layout_width">@dimen/size_0dp</item>
        <item name="android:layout_height">wrap_content</item>

</style>
    <style name="hint_appearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">#808080</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
